# Mountiva static export (Hostinger) — Apache config.
# Mirrors the security headers set in next.config.mjs for the Node build.

<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
  # Hostinger issues free SSL by default; safe once the domain only ever
  # serves over HTTPS. Remove this line if you ever need to serve plain HTTP.
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
</IfModule>

# Next.js exports every route as a directory + index.html (trailingSlash),
# so Apache's default DirectoryIndex serves clean URLs with no rewriting.

# Custom 404.
ErrorDocument 404 /404.html

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/avif "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
</IfModule>
